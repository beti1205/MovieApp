<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="episode"
            type="com.example.movieplayer.feature.fetchtvepisodes.data.Episode" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="8dp">

        <TextView
            android:id="@+id/episodeNumber"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{@string/episode_number(episode.numberOfEpisode)}"
            android:textAppearance="?attr/textAppearanceBody1"
            app:layout_constraintEnd_toEndOf="@id/poster"
            app:layout_constraintStart_toStartOf="@id/poster"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Episode 1" />

        <ImageView
            android:id="@+id/poster"
            imageUrl="@{episode.posterPath}"
            android:layout_width="300dp"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:paddingTop="8dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/episodeNumber"
            tools:srcCompat="@drawable/poster" />

        <TextView
            android:id="@+id/name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:paddingTop="8dp"
            android:textAlignment="center"
            android:text="@{episode.name}"
            android:textAppearance="?attr/textAppearanceSubtitle1"
            android:textColor="?attr/colorSecondary"
            app:layout_constraintStart_toStartOf="@id/poster"
            app:layout_constraintEnd_toEndOf="@id/poster"
            app:layout_constraintTop_toBottomOf="@id/poster"
            tools:text="Name" />

        <TextView
            android:id="@+id/episodeAirDate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{episode.episodeAirDate}"
            android:textAppearance="?attr/textAppearanceCaption"
            app:layout_constraintEnd_toEndOf="@id/poster"
            app:layout_constraintTop_toBottomOf="@id/name"
            tools:text="2011-04-17" />

        <TextView
            android:id="@+id/episodeOverview"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:justificationMode="inter_word"
            android:text="@{episode.overview}"
            android:textAppearance="?attr/textAppearanceBody2"
            app:layout_constraintEnd_toEndOf="@+id/poster"
            app:layout_constraintStart_toStartOf="@id/poster"
            app:layout_constraintTop_toBottomOf="@id/episodeAirDate"
            tools:text="About episode..." />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>