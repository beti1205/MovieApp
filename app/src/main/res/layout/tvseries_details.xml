<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="com.example.movieplayer.ui.details.tvseries.TVSeriesDetailsViewModel" />

        <variable
            name="season"
            type="com.example.movieplayer.feature.fetchtvseriesdetails.data.Season" />
    </data>

    <androidx.core.widget.NestedScrollView
        android:id="@+id/nestedScrollView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:transitionName="@string/tv_card_detail_transition_name">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ImageView
                android:id="@+id/imageView"
                android:layout_width="match_parent"
                android:layout_height="600dp"
                app:imageUrl="@{viewModel.selectedTVSeries.posterPath}"
                app:layout_constrainedHeight="true"
                app:layout_constraintBottom_toTopOf="@id/textContent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintVertical_bias="0"
                tools:srcCompat="@drawable/poster" />

            <androidx.constraintlayout.widget.ConstraintLayout
                android:id="@+id/textContent"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:padding="8dp"
                app:layout_constraintTop_toBottomOf="@id/imageView">

                <TextView
                    android:id="@+id/title"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:gravity="start"
                    android:text="@{viewModel.selectedTVSeries.name}"
                    android:textColor="?attr/colorSecondary"
                    android:textAppearance="?attr/textAppearanceHeadline6"
                    app:layout_constraintBottom_toTopOf="@id/releaseData"
                    tools:text="Title" />

                <TextView
                    android:id="@+id/releaseData"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:paddingBottom="8dp"
                    android:text="@{viewModel.selectedTVSeries.firstAirDate}"
                    android:textColor="@color/grey"
                    android:textAppearance="?attr/textAppearanceCaption"
                    app:layout_constraintEnd_toStartOf="@+id/voteAverage"
                    app:layout_constraintHorizontal_bias="0.5"
                    app:layout_constraintHorizontal_chainStyle="spread_inside"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toBottomOf="@id/title"
                    tools:text="2000" />

                <ImageView
                    android:id="@+id/star"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:paddingEnd="4dp"
                    app:layout_constraintEnd_toStartOf="@id/voteAverage"
                    app:layout_constraintTop_toBottomOf="@id/title"
                    app:srcCompat="@drawable/ic_baseline_stars_24"
                    app:tint="@color/orange" />

                <TextView
                    android:id="@+id/voteAverage"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:paddingBottom="8dp"
                    android:text="@{viewModel.selectedTVSeries.votes}"
                    android:textAppearance="?attr/textAppearanceSubtitle1"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintHorizontal_bias="0.5"
                    app:layout_constraintStart_toEndOf="@+id/releaseData"
                    app:layout_constraintTop_toBottomOf="@id/title"
                    tools:text="3.0" />

                <TextView
                    android:id="@+id/overview"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:paddingBottom="8dp"
                    android:textAppearance="?attr/textAppearanceBody2"
                    android:text="@{viewModel.selectedTVSeries.overview}"
                    app:layout_constraintTop_toBottomOf="@id/releaseData"
                    tools:text="About Movie" />

                <com.google.android.material.divider.MaterialDivider
                    android:id="@+id/divider"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    app:layout_constraintTop_toBottomOf="@id/overview" />

                <com.google.android.material.textfield.TextInputLayout
                    android:id="@+id/menu"
                    style="@style/ExposedDropdownMenu"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:hint="Season"
                    android:paddingTop="8dp"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toBottomOf="@id/overview">

                    <AutoCompleteTextView
                        android:id="@+id/seasonAutoCompleteTextView"
                        filter="@{false}"
                        onItemClicked="@{(parent, view, position, id) -> viewModel.setSelectedSeasonPosition(position)}"
                        seasons="@{viewModel.seasons}"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:inputType="none"
                        android:text="@{viewModel.selectedSeason.name}" />

                </com.google.android.material.textfield.TextInputLayout>

                <ImageView
                    android:id="@+id/seasonPoster"
                    imageUrl="@{viewModel.selectedSeason.posterPath}"
                    android:layout_width="140dp"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:adjustViewBounds="true"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toBottomOf="@+id/menu"
                    tools:srcCompat="@drawable/poster" />

                <TextView
                    android:id="@+id/seasonAirDate"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="8dp"
                    android:text="@{viewModel.selectedSeason.airDate}"
                    android:textAppearance="?attr/textAppearanceBody2"
                    app:layout_constraintBottom_toTopOf="@id/seasonEpisodesCount"
                    app:layout_constraintStart_toEndOf="@id/seasonPoster"
                    app:layout_constraintTop_toTopOf="@id/seasonPoster"
                    app:layout_constraintVertical_chainStyle="packed"
                    tools:text="Air Date: 12.08.2022" />

                <TextView
                    android:id="@+id/seasonEpisodesCount"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@{@string/episodes_count(viewModel.selectedSeason.episodes())}"
                    android:textAppearance="?attr/textAppearanceBody2"
                    app:layout_constraintBottom_toTopOf="@id/seasonOverview"
                    app:layout_constraintStart_toStartOf="@id/seasonAirDate"
                    app:layout_constraintTop_toBottomOf="@id/seasonAirDate"
                    tools:text="Episodes: 124" />

                <TextView
                    android:id="@+id/seasonOverview"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:text="@{viewModel.selectedSeason.overview}"
                    android:textAppearance="?attr/textAppearanceBody2"
                    app:layout_constraintBottom_toTopOf="@id/secondDivider"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintStart_toStartOf="@id/seasonEpisodesCount"
                    app:layout_constraintTop_toBottomOf="@id/seasonEpisodesCount"
                    tools:text="Season overview" />

                <androidx.constraintlayout.widget.Barrier
                    android:id="@+id/barrier"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    app:barrierDirection="bottom"
                    app:constraint_referenced_ids="seasonOverview, seasonPoster" />

                <com.google.android.material.divider.MaterialDivider
                    android:id="@+id/secondDivider"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    app:layout_constraintTop_toTopOf="@id/barrier" />

                <TextView
                    android:id="@+id/seasonName"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@{viewModel.selectedSeason.name}"
                    android:textAppearance="?attr/textAppearanceSubtitle1"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toBottomOf="@id/secondDivider" />

                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/episodesRecyclerView"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:orientation="horizontal"
                    app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toBottomOf="@id/seasonName"
                    tools:listitem="@layout/episode_item" />

            </androidx.constraintlayout.widget.ConstraintLayout>

        </androidx.constraintlayout.widget.ConstraintLayout>

    </androidx.core.widget.NestedScrollView>

</layout>